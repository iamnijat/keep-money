abstract class DurationsUtil {
  const DurationsUtil._();

  static const oneHundredMillisecondsDuration = Duration(milliseconds: 100);
  static const twoHundredFiftyMillisecondsDuration =
      Duration(milliseconds: 250);
  static const threeHundredMillisecondsDuration = Duration(milliseconds: 300);
  static const fourHundredMillisecondsDuration = Duration(milliseconds: 400);
  static const sixHundredMillisecondsDuration = Duration(milliseconds: 600);

  static const oneSecondDuration = Duration(seconds: 1);
}
