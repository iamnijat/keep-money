part of modal_bottom_sheets;

class PersonalInfoEditModalBottomSheetDivider extends StatelessWidget {
  const PersonalInfoEditModalBottomSheetDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Divider(color: AppPalette.thirdGreyColor, thickness: 1);
  }
}
